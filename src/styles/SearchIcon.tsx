import styled from '@emotion/styled'
import { colors } from './defaults/variables'

export default styled.span`
  color: ${colors.accent};
  position: relative;
  font-size: 1.4rem;
  right: 1.5rem;
  top: 0.3rem;
`
