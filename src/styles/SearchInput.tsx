import styled from '@emotion/styled'

export default styled.input`
  border: none;
  border-radius: 4px;
  height: 2.5rem;
  min-width: 24rem;
  margin-right: -19px;

  &::placeholder {
    padding-left: 1rem;
  }
`
